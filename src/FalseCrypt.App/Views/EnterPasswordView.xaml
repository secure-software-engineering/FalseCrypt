<mafc:DialogWindow x:Class="FalseCrypt.App.Views.EnterPasswordView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FalseCrypt.App.Views"
        xmlns:mafc="http://modern.framework.com/controls"
        mc:Ignorable="d"
        Title="EnterPasswordView" SizeToContent="Height" Width="300">
    <Grid Margin="12,0,12,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Password:"></TextBlock>
        <Grid Grid.Row="1" Margin="0,6,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox HorizontalAlignment="Stretch" Text="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Grid.Column="2" Content="OK" MinWidth="75" Padding="12,0,12,0" Command="{Binding EnterPasswordCommand}"/>
        </Grid>
    </Grid>
</mafc:DialogWindow>
